{
  "name": "livoice",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n web,api,worker -c \"cyan,magenta,green\" \"npm run dev:web\" \"npm run dev:api\" \"npm run dev:worker\"",
    "dev:web": "npm run dev --workspace=livoice-web",
    "dev:api": "npm run dev --workspace=livoice-api",
    "dev:worker": "npm run worker:dev --workspace=livoice-api",
    "clean": "rm -rf node_modules packages/*/node_modules packages/*/package-lock.json packages/*/yarn.lock package-lock.json yarn.lock"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "next": "14.2.33",
    "prettier": "3.5.3"
  },
  "overrides": {
    "next": "14.2.33",
    "livoice-web": {
      "react": "^19.1.0",
      "react-dom": "^19.1.0"
    }
  },
  "engines": {
    "node": ">=22.18.0",
    "npm": ">=10.x"
  }
}
