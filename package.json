{
  "name": "livoice",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=livoice-web & npm run dev --workspace=livoice-api",
    "dev:web": "npm run dev --workspace=livoice-web",
    "dev:api": "npm run dev --workspace=livoice-api",
    "clean": "rm -rf node_modules packages/*/node_modules packages/*/package-lock.json packages/*/yarn.lock package-lock.json yarn.lock"
  },
  "devDependencies": {
    "next": "14.2.33",
    "prettier": "3.5.3"
  },
  "engines": {
    "node": ">=22.18.0",
    "npm": ">=10.x"
  }
}
