{
  "name": "pto",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=pto-web & npm run dev --workspace=pto-api",
    "dev:web": "npm run dev --workspace=pto-web",
    "dev:api": "npm run dev --workspace=pto-api",
    "clean": "rm -rf node_modules packages/*/node_modules packages/*/package-lock.json packages/*/yarn.lock package-lock.json yarn.lock"
  },
  "devDependencies": {
    "next": "14.2.33",
    "prettier": "3.5.3"
  },
  "overrides": {
    "next": "14.2.33",
    "pto-web": {
      "react": "^19.1.0",
      "react-dom": "^19.1.0"
    }
  },
  "engines": {
    "node": ">=22.18.0",
    "npm": ">=10.x"
  }
}
