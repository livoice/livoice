mutation CreateChatConfig($data: ChatConfigCreateInput!) {
  createChatConfig(data: $data) {
    id
    name
    notes
    systemPrompt
    openai
    context
    segments
    createdAt
    updatedAt
  }
}

