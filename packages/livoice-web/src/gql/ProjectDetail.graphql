query ProjectDetail($projectId: ID!) {
  project(where: { id: $projectId }) {
    id
    name
    description
    org {
      id
      name
    }
    sources {
      id
      name
      type
      importStatus
      transcriptsCount
    }
  }
}
