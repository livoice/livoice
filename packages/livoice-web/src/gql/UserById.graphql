query GetUserById($id: ID!) {
  user(where: { id: $id }) {
    id
    firstName
    lastName
    email
    displayName
    role
    location {
      id
      name
    }
    isActive
    startDate
  }
}
