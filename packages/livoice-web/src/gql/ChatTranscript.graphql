mutation ChatTranscript($input: ChatTranscriptInput!) {
  chatTranscript(input: $input) {
    chatId
    answer
    messages {
      id
      role
      content
      createdAt
    }
    references {
      id
      text
      startMs
      endMs
      speaker
      transcriptTitle
    }
  }
}
