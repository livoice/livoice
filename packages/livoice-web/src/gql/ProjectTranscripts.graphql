query ProjectTranscripts($projectId: ID!) {
  project(where: { id: $projectId }) {
    id
    sources {
      id
      name
      transcripts {
        id
        title
        intervieweeName
        createdAt
      }
    }
  }
}
