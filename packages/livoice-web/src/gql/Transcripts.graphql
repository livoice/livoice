query Transcripts($sourceId: ID) {
  transcripts(where: { source: { id: { equals: $sourceId } } }) {
    id
    title
    intervieweeName
    notes
    createdAt
    segmentsCount
    source {
      id
      name
    }
  }
}
