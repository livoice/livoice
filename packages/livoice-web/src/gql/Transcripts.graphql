query Transcripts($projectId: ID) {
  transcripts(where: { project: { id: { equals: $projectId } } }) {
    id
    title
    intervieweeName
    notes
    createdAt
    segmentsCount
    project {
      id
      name
    }
  }
}

