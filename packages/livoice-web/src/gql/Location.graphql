query Location($id: ID!) {
  location(where: { id: $id }) {
    id
    name
    timezone
    workingDays
    weekStartDay
    holidayCountry
    usersCount
    org {
      id
      locationsCount
    }
  }
}
